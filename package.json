{
  "name": "sdk",
  "version": "0.0.3",
  "private": true,
  "repository": "https://github.com/storyprotocol/sdk",
  "author": "storyprotocol engineering <eng@storyprotocol.xyz>",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "fix": "turbo run fix",
    "test": "turbo run test --no-cache",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "husky": "^8.0.0",
    "turbo": "^1.10.13"
  },
  "engines": {
    "node": "^20.0.0"
  }
}
